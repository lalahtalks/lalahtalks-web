function CreateSecretSubmit(props) {

    return (
        <button className="btn btn-primary" onClick={props.onClicked}>
            Submit
        </button>
    );

}

export default CreateSecretSubmit;
